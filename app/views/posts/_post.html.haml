%article
  %header
    %h2= post.title
    %h3= "Author: #{post.authors} Published: #{post.published_on}"
    =comments_link(post)

  %section
    = post.content
    %p#tags
      = "Tags : #{post.get_tags || 'None'}"

  %footer= post_update_destroy_links(post)
